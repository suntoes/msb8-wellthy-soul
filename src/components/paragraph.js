import styled from '@emotion/styled'

const Paragraph = styled.p`
  white-space: pre-wrap;
  text-align: justify;
`
//  text-indent: ${({initial=true}) => initial ? "1em" : "0em"};

export default Paragraph
